---
title: "Dashboards und Interaktivität"
description: "Was Interaktion bringt, was sie kostet, und wie man Dashboards ordnet."
categories: ["Dashboards", "Interaktivität", "Visualisierung", "Deployment"]
quelle: ["DAVI"]
seitentyp: werkzeug
stand: entwurf
order: 90
---

## Kernideen

- Drei Auslieferungsformen: statisch, interaktiv, dialogisch
- Statisch hat eine Aussage, interaktiv erlaubt Fragen, dialogisch beantwortet freie Fragen
- Interaktion verschiebt Arbeit zum Publikum und ist deshalb rechtfertigungspflichtig
- Ein Dashboard ohne Entscheidungsfrage wird zur Kennzahlentapete
- Die wichtigste Zahl gehört oben links, alles Weitere darunter
- Wer ein Dashboard baut, übernimmt Pflege, nicht nur Erstellung

## Erklärung

::: {.note-box}
**Vorwissen:** [Zielbild und Publikum](zielbild-audience.qmd) für die
Entscheidungsfrage und [Diagrammtyp wählen](diagrammtyp-waehlen.qmd) für die
einzelnen Kacheln. Die technische Seite steht unter
[seaborn und plotly](seaborn-plotly.qmd) sowie
[Reproduzierbarkeit](../programmierung/reproduzierbarkeit.qmd).
:::

### Drei Formen der Auslieferung

| Form | Was das Publikum tut | Wann passend | Kosten |
|---|---|---|---|
| Statisch | liest | Die Aussage steht fest und gilt für alle | gering, Pflege nur bei neuen Daten |
| Interaktiv | filtert, zoomt, wählt aus | Verschiedene Leute haben verschiedene Fragen an dieselben Daten | Aufbau und laufender Betrieb |
| Dialogisch | fragt in eigenen Worten | Fragen sind nicht vorhersehbar | hoch, dazu Prüfbarkeit der Antworten |

Der häufigste Fehlgriff ist die interaktive Form für eine feststehende Aussage.
Wer weiss, was gezeigt werden soll, zeigt es; ein Filtermenü davor ist keine
Freiheit, sondern eine Hausaufgabe für das Publikum.

### Wann Interaktion wirklich hilft

Interaktion lohnt sich, wenn sie eine echte Frage beantwortet:

- **Detail auf Abruf:** Der Überblick zeigt das Muster, der Klick zeigt den
  Einzelfall. Klassisch für Karten und Zeitreihen.
- **Verschiedene Zuschnitte:** Vertrieb, Produktion und Leitung sehen dieselben
  Daten mit verschiedenen Filtern, und die Grafik bleibt dieselbe.
- **Grosse Datenmengen:** Zoomen ersetzt zwanzig vorbereitete Ausschnitte.
- **Vergleich nach Wahl:** Das Publikum entscheidet, welche zwei Serien
  nebeneinander gehören.

Sie lohnt sich nicht als Dekoration. Animationen, die nur laufen, weil es geht,
verbrauchen Aufmerksamkeit und Ladezeit, und der Zusammenhang zwischen Bildern
lässt sich schlechter erinnern als der Vergleich nebeneinander.

### Was ein Dashboard beantwortet

Ein Dashboard ist kein Ort, an dem alle verfügbaren Kennzahlen gezeigt werden,
sondern die laufende Antwort auf eine wiederkehrende Entscheidungsfrage. Vor dem
Bauen gehören drei Dinge festgelegt:

1. **Die Frage.** "Läuft die Produktion im Rahmen?" ist eine Frage.
   "Produktionskennzahlen" ist keine.
2. **Der Takt.** Wird täglich, wöchentlich oder monatlich geschaut? Das
   bestimmt die Aggregation und was als Abweichung gilt.
3. **Die Reaktion.** Was passiert, wenn eine Kennzahl ausschlägt? Wenn es darauf
   keine Antwort gibt, gehört die Kennzahl nicht auf das Dashboard.

### Aufbau

Der Blick beginnt oben links und wandert nach rechts und unten. Daraus folgt
eine schlichte Ordnung:

- **Oben links:** die eine Kennzahl, um die es geht, gross, mit Vergleichswert
  (Vorperiode, Ziel, Schwelle).
- **Obere Reihe:** zwei bis drei begleitende Kennzahlen im selben Format.
- **Mitte:** der Verlauf, der zeigt, wie es dazu kam.
- **Unten:** die Aufschlüsselung nach Gruppe, Region oder Produkt.
- **Rand:** Filter, Stand der Daten, Quelle.

Weitere Regeln, die sich bewähren:

- Eine Bildschirmhöhe. Was gescrollt werden muss, wird nicht gesehen.
- Einheitliche Skalen über vergleichbare Kacheln, sonst täuscht der Vergleich.
- Kein Ampelrot ohne definierte Schwelle. Farbe ohne Regel ist Meinung.
- Zahlen mit Einheit und Bezug, nie nackt.

### Was danach kommt: Betrieb

Ein Dashboard ist eine Anwendung, keine Abgabe. Damit übernimmt man drei
Aufgaben dauerhaft: die Datenzufuhr am Laufen halten, auf Strukturänderungen in
der Quelle reagieren, und Kennzahlen entfernen, die niemand mehr anschaut.

Die häufigste Krankheit von Selbstbedienungs-Werkzeugen ist der Wildwuchs:
dieselbe Kennzahl in drei Varianten, jede leicht anders gerechnet, und niemand
weiss, welche gilt. Dagegen hilft nur eine Stelle, an der die Definition
festgeschrieben ist, und der Verzicht auf Kennzahlen ohne Verantwortlichen.

::: {.quiz}
Ein Bericht soll monatlich an die Bereichsleitung gehen, die Aussage steht
jeweils fest. Welche Form ist richtig?

- [x] Statisch, mit der Aussage im Titel ~ Richtig. Wenn feststeht, was gezeigt
      werden soll, ist Interaktion nur zusätzlicher Aufwand für beide Seiten.
      Ein Dashboard lohnt sich, wenn verschiedene Leute verschiedene Fragen an
      dieselben Daten haben.
- [ ] Interaktiv, damit sich jeder vertiefen kann ~ Vertiefung wird selten
      genutzt und verschiebt die Deutungsarbeit zum Publikum.
- [ ] Dialogisch, das ist am flexibelsten ~ Flexibel und am aufwendigsten, und
      die Antworten müssten geprüft werden.
:::

::: {.quiz}
Auf einem Dashboard steht eine Kennzahl, für die es keine definierte Reaktion
gibt. Was folgt?

- [ ] Sie bleibt als Kontext stehen ~ Kontext ist ein Vorwand; sie verbraucht
      Platz und Aufmerksamkeit.
- [x] Sie gehört nicht auf das Dashboard ~ Richtig. Ein Dashboard stützt
      Entscheidungen. Eine Kennzahl ohne mögliche Reaktion ist Dekoration und
      verwässert die, auf die es ankommt.
- [ ] Sie braucht eine Ampel ~ Eine Ampel ohne definierte Schwelle macht es
      schlimmer.
:::

## Typische Aufgaben

**Ein Dashboard mit Quarto aufsetzen**

```yaml
---
title: "Produktion"
format:
  dashboard:
    orientation: rows
---
```

```markdown
## Row {height=25%}

::: {.valuebox icon="speedometer" color="primary"}
Ausbringung heute

`{r} ausbringung`
:::

## Row {height=75%}

### Column
Verlauf der letzten 30 Tage

### Column
Aufschlüsselung nach Linie
```

**Eine interaktive Grafik einbetten**

```python
import plotly.express as px
abb = px.line(df, x="datum", y="wert", color="linie", template="simple_white")
abb.write_html("verlauf.html", include_plotlyjs="cdn")
```

**Eine Tabelle durchsuchbar machen**

```r
DT::datatable(df, options = list(pageLength = 10, dom = "ftip"))
```

**Filter mit Shiny, wenn Interaktion wirklich nötig ist**

```r
ui <- fluidPage(
  selectInput("standort", "Standort", choices = unique(df$standort)),
  plotOutput("verlauf")
)

server <- function(input, output) {
  output$verlauf <- renderPlot({
    ggplot(subset(df, standort == input$standort), aes(datum, wert)) +
      geom_line()
  })
}
```

Shiny braucht einen laufenden R-Prozess, also einen Server. Wo eine
HTML-Datei genügt, ist sie die einfachere und haltbarere Lösung.

**Den Stand der Daten sichtbar machen**

```r
paste("Datenstand:", format(max(df$datum), "%d.%m.%Y"),
      "| Quelle: Produktionsdatenbank")
```

Diese eine Zeile verhindert die häufigste Rückfrage und die häufigste
Fehlentscheidung auf Basis alter Zahlen.

## Verlinkte Ressourcen

- [Quarto Dashboards](https://quarto.org/docs/dashboards/)
- [Shiny: Einführung](https://shiny.posit.co/r/getstarted/)
- [Stephen Few über Dashboard-Gestaltung](https://www.perceptualedge.com/library.php)
